# Skill: Generador de Datos Híbrido

Este paquete extrae la lógica de generación de datos (Faker) y la integración con Inteligencia Artificial (Google Gemini) en un módulo independiente instalable vía `pip`.

## Instalación

```bash
pip install git+https://github.com/mariela3009/skill-generador-datos.git@main
```

## Uso

```python
from skill_generador_datos import DataGenerator, generate_seed_data_for_database

# ... lógica de tu aplicación ...
```
